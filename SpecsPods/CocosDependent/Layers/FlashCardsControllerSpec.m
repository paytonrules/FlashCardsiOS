#import <OCDSpec2/OCDSpec2.h>
#import <OCMock/OCMock.h>
#import "FlashCardsController.h"
#import "Card.h"

OCDSpec2Context(FlashCardsControllerSpec) {
  
  Describe(@"Observing changes to the Flashcards", ^{
    
    It(@"schedules a playClue when an observed card becomes current", ^{
  /*    id gameView = [OCMockObject mockForProtocol:@protocol(GameView)];
      Card *card = [[Card new] autorelease];
      NSMutableArray *cards = [[NSMutableArray array] autorelease];
      FlashCardsController *cont = [FlashCardsController contWith:cards andView:gameView];*/
      
   //   [cards addObject:card];
      
    //  [card makeCurrent];
      
      
      //[cont observeValueForKeyPath:<#(NSString *)#> ofObject:<#(id)#> change:<#(NSDictionary *)#> context:<#(void *)#>]
    });
    
  });
  
}
